name: cerber_retry_demo
description: Validator that retries until a threshold attempt, or fails when forced.

input_schema: null
output_schema: null

serialize:
  enabled: false
  base_dir: null
  per_step: false

entry_step_id: decide
end_step_ids:
  - decide

steps:
  decide:
    kind: tool
    tool_name: retry_validator
    tool_params: {}
    save_mapping:
      validation: validation
    validator_agent_name: null
    validator_params: {}
    validator_policy: {}
    transitions: []
