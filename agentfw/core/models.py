"""Legacy core models placeholder.

Перенесено до `agentfw._legacy.core.models`. Замість цього використовуйте `agentfw.core.agent_spec`.
"""

raise RuntimeError("Legacy removed; use AgentSpec/engine")
